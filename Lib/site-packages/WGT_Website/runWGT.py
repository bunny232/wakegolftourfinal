import webbrowser
import os
import sys

command = 'start python manage.py runserver'
os.system(command)

url = 'http://localhost:8000/'

arg = ""
if len(sys.argv) >= 2:
    arg = sys.argv[1]

url = url + arg

webbrowser.open(url)
